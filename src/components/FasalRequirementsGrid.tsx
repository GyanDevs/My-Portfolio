"use client"; import React from 'react'; interface FasalRequirementsGridProps { items: string[]; label: string;
} /** * FasalRequirementsGrid * Displays goals or constraints in a refined grid format for the IoT Case Study. */
export default function FasalRequirementsGrid({ items, label }: FasalRequirementsGridProps) { return ( <div className="w-full grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 bg-background border-t border-l border-[var(--grid-line)] mb-12"> {items.map((item, idx) => {
// Try to split "**Title:** Description"
const parts = item.split(/(\*\*.*?\*\*)/); let title = ""; let description = item; if (parts.length >= 3 && parts[1].startsWith('**')) { title = parts[1].replace(/\*\*/g, '').replace(/:$/, '').trim(); description = parts.slice(2).join('').replace(/^:\s*/, '').trim(); } return ( <div key={idx} className="p-8 flex flex-col gap-8 group card-hover transition-colors border-r border-b border-[var(--grid-line)]" > <div className="flex justify-between items-start"> <p className="font-mono text-[13px] uppercase tracking-[0.2em] text-neutral-500 dark:text-neutral-400 font-bold">{label}</p> <span className="font-mono text-[13px] text-neutral-400">{(idx + 1).toString().padStart(2, '0')}</span> </div> <div> {title && ( <h4 className="text-xl font-bold tracking-tight mb-3 leading-tight text-[var(--foreground)]"> {title} </h4> )} <p className="text-[16px] text-neutral-500 dark:text-neutral-400"> {description} </p> </div> </div> ); })} </div> );
}
